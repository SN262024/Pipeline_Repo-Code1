name: "my first workflow"
on: 
  branches:
    push:
     -'features/**'
     - 'main'
  jobs:
    terraformjob
      runs-on:surendra-runner
      steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0

